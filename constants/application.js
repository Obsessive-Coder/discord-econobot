const READY_MESSAGE = 'Economy bot is ready.';
const COMMAND_ERROR_MESSAGE = 'There was an error trying to execute that command!';

module.exports.READY_MESSAGE = READY_MESSAGE;
module.exports.COMMAND_ERROR_MESSAGE = COMMAND_ERROR_MESSAGE;

module.exports = {
  READY_MESSAGE,
  COMMAND_ERROR_MESSAGE,
};
